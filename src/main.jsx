import React from 'react'
import ReactDOM from 'react-dom/client'
import GerenciadorFaltas from './components/GerenciadorFaltas'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GerenciadorFaltas />
  </React.StrictMode>,
)
